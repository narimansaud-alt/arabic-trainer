# Medina rules: source-only and maximum-example override

Recorded: 2026-08-12.

This instruction has priority over earlier abbreviated extraction descriptions.

- Use only the Arabic sharhs explicitly supplied by the owner as evidentiary sources. Do not establish or supplement rules from model knowledge, the internet, translations, or unrelated teaching materials.
- For Book 2, read and compare both supplied sharhs lesson by lesson:
  - `Podrobny_Sharkh_2_tom.pdf` (80 PDF pages);
  - `Sharkh_na_2_tom_Med_kursa.pdf` (62 PDF pages).
- Add source-backed material from both sharhs to the same lesson.
- When two explanations or examples are equivalent, merge them without creating a duplicate public card or duplicate example.
- Preserve source-specific additions and report differences between the two sharhs for every lesson.
- Include the maximum useful set of examples present in both sharhs, not merely one representative example. Every public Arabic example must be fully and correctly vocalized and accompanied by its Russian meaning.
- Keep `rule_ar` and `source_text` separate. `rule_ar` is the concise fully vocalized formulation; every `source_text` is a separate literal visual transcription associated with its exact PDF and page range.
- Manual visual transcription from the 62-page PDF is explicitly authorized because its embedded Arabic text layer is technically corrupted. It must be checked against the rendered page and must not contain invented wording.
- Continue autonomously through the prescribed order and record a before/after report for every lesson in the permanent project progress log.
