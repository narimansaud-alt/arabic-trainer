# Второй том — завершение сверки урока 6 по второму шарху

Дата: 2026-08-13.

## Прочитанные границы

- `Sharkh_na_2_tom_Med_kursa.pdf`: PDF-страницы 14–17 полностью.
- PDF-страница 18 начинается заголовком урока 7.

## Найдено до исправления

- Не было двойственного `ـهُمَا`, полного ряда объектных местоимений, различия `ـتَ / ـتِ / ـتُ` и восьми вопросно-ответных примеров.
- В карточке `أَظُنُّ` отсутствовали десять форм с `أَنَّ`.
- Полностью отсутствовали обзор числительных `١١–١٩` и десятков `٢٠–٩٠`, а также правило `أَنْوَاعُ الْإِعْرَابِ`.
- Карточки `لِمَهْ؟` и `هَاتِ` не содержали всех примеров второго шарха.
- Два шарха по-разному описывают множественное моделей `فَعْلَانَ / فَعْلَى`.

## Итоговый состав

1. Полная классификация местоимений, формы `تَاءُ الْفَاعِلِ`, ответы и ряд `ـهُ، ـهَا، ـهُمَا، ـهُمْ، ـهُنَّ`.
2. Три синтаксические позиции `نَا`.
3. `أَظُنُّ` с двумя дополнениями и полным рядом `أَنَّ`.
4. Составные числительные и названия десятков со всеми примерами страниц 15–16.
5. `فَعْلَانَ / فَعْلَى` с раздельно показанными формулировками двух шархов.
6. `لِمَ؟ / لِمَهْ؟` со всеми диалогами.
7. Три вида и‘раба имён, исходные показатели и полная таблица.
8. `هَاتِ` со всеми формами и обращениями.

## Различие шархов

- Подробный шарх задаёт основную модель множественного `فِعَالٌ` и называет `كُسَالَى` исключением.
- Второй шарх прямо перечисляет две модели `فِعَالٌ وَفُعَالَى` и добавляет `سُكَارَى`.
- Обе позиции сохранены отдельно; внешнее решение за источники не подставлялось.

## База и проверки

- Миграция: `supabase/migrations/20260813032000_supplement_book2_lesson6_from_second_sharh.sql`.
- Рабочая Supabase обновлена.
- Итог: 8 карточек; добавлены 8 отдельных транскрипций второго шарха.
- Пустых `rule_ar`, переводов заголовков, неогласованных публичных фрагментов, незакрытых тегов и неадаптивных таблиц: 0.
